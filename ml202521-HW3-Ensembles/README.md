# HW 3. Ensembles.

В этом домашнем задании вам требуется написать методы ансамблирования для решения задачи многоклассовой классификации. Для решения задания допускается использование библиотек `numpy`, `pandas` и `scikit-learn`. Решение сдается коммитом в этот репозиторий заполненного файла `solution.py`. За задание максимально можно набрать **40 баллов**. Вес каждого пункта указан в самом задании.

:exclamation: **Использование других библиотек приведет к завершению проверки со статусом `runtime error`, что соответствует 0 баллов за ДЗ. Для устранения этой ошибки достаточно перезалить решение с использованием только разрешенных библиотек.**

:exclamation: **После дедлайна ваши решения будут протестированы на плагиат и на факт использования ИИ. Штрафные санкции будут применяться в зависимости от степени плагиата и могут доходить до полного аннулирования ДЗ.**

:exclamation: **<span style="color:red"> Изменение каких либо файлов в этом репозитории кроме файлов `solution.py` и `README.md` приведет к статусу проверки `too many files modified`, что соответствует 0 баллов за ДЗ. Что с этим делать читайте [здесь](#problems). </span>**

После каждого изменения в этом репозитории будет запускаться автоматическая проверка. Вы так же можете запускать локальные проверки, если склонируете этот репозиторий к себе командой:
```
git clone https://github.com/mipt-ml-course/ml202521-HW3-Ensembles.git
cd ml202521-HW3-Ensembles
```
Для клонирования репозитория требуется выпустить github токен, если у Вас его еще нет. Сделать это можно по [инструкции](https://docs.google.com/presentation/d/1oSRp_wQGbCcMtvH92m3O8P0pLzaZeI5vHkzDQMUSaG4/edit?usp=sharing). Команды запуска локальных проверок приведены ниже в описании заданий.

### Задание

В задании требуется вписать код в местах `# CODE HERE` и `...`.

#### Random Forest (10 баллов)
   * Напишите ансамбль типа `RandomForest` для решения задачи классификации. 
   * Подберите оптимальные гиперпараметры модели и установите их в качестве значений по умолчанию. Проверьте свое решение с помощью теста.
   
> Подобранная конфигурация ансамбля должна проходить все тесты задания.

```python run.py unittest random_forest```
      
#### Blending (10 баллов)
   * Напишите ансамбль типа `Blending` для решения задачи классификации. 
   * Подберите оптимальный набор из **не менее трех** базовых моделей и мета-модель. Установите их в качестве значений по умолчанию. Проверьте свое решение с помощью теста.
   
> Подобранная конфигурация ансамбля должна проходить все тесты задания.
> Для факторного пространства мета-модели используйте метод `predict_proba` базовых моделей.
> В качестве базовых моделей и мета-модели используйте модели из библиотеки `scikit-learn`.

```python run.py unittest blending```

#### Stacking (10 баллов)
   * Напишите ансамбль типа `Stacking` для решения задачи классификации. 
   * Подберите оптимальный набор из **не менее трех** базовых моделей и мета-модель. Установите их в качестве значений по умолчанию. Проверьте свое решение с помощью теста.
> Подобранная конфигурация ансамбля должна проходить все тесты задания.
> Для факторного пространства мета-модели используйте метод `predict_proba` базовых моделей.
> В качестве базовых моделей и мета-модели используйте модели из библиотеки `scikit-learn`.
> Для инференса модели используйте базовые модели, обученные на **ВСЁМ** датасете.

```python run.py unittest stacking```

#### Boosting (10 баллов)
   * Напишите ансамбль типа `Boosting` для решения задачи классификации. 
   * Подберите оптимальные гиперпараметры модели и установите их в качестве значений по умолчанию. Проверьте свое решение с помощью теста.
   
> Подобранная конфигурация ансамбля должна проходить все тесты задания.
> Каждое дерево предсказывает логиты. Для подсчета вероятностей используйте softmax. 
> Результирующие логиты инициализируйте нулями.
> Для оптимизации используйте функционал cross-entropy.

```python run.py unittest boosting```

### Загрузка решения:

После локального тестирования решения, отправьте свое решение в ваш GitHub репозиторий с помощью команды ниже или с помощью интерфейса Github для загрузки файлов.

```
git add solution.py
git commit -m 'commit solution'
git push
```

После загрузки решения в репозиторий рядом с названием репозитория появится оранжевая точка, которая означает, что система начала проверку вашего решения. Когда желтая точка сменится зеленной галочкой, значит, что проверка окончена. Результаты проверки будут записаны в summary проверки. Файл summary можно найти по [инструкции](https://docs.google.com/presentation/d/15Ned2Ew684tzvRX__VhJ0okxcvZsuiJGgY1kzaPrYtI/edit?usp=sharing).

### Проблемы с загрузкой: <a name="problems"></a>

Если после проверки решения у вас в таблице висит статус `too many files modified`, значит вы изменили файлы какие-то файлы, кроме `solution.py`и `README.md`. Вам следует найти и удалить коммит в которым вы изменили что-то лишнее. Для этого в своем репозитории на GitHub нажмите на кнопку `commits` в правом верхнем углу репозитория, найдите коммит в списке коммитов, запомните его порядковый номер сверху, локально на своем устройстве введите следующие команды, где вместо N напишите порядковый номер коммита, который нужно удалить:
```
git pull
git reset --hard HEAD~N
git push -f
```
В результате будет удалено N первых коммитов. Повторно произведите загрузку решения. Обратите внимание, что все файлы, включая `solution.py`, будут сброшены до состояния N-1 коммита. 

:exclamation: **<span style="color:red">НЕ ЗАБУДЬТЕ СОХРАНИТЬ СВОЕ РЕШЕНИЕ В ДРУГОЙ ПАПКЕ, ПРЕЖДЕ ЧЕМ УДАЛЯТЬ КОММИТЫ.</span>**

По возникающим вопросам можно писать в телеграм @ex_lex или на почту zhavoronkin.ao@phystech.edu.
